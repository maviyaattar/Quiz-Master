/* ============================================
   CONFIG.EXAMPLE.JS - Configuration Template
   Maviya Attar - Quiz Master Platform
   ============================================ */

/**
 * SETUP INSTRUCTIONS:
 * AI quiz generation is now handled securely by the backend.
 * No API keys are needed in the frontend anymore.
 * 
 * If you need to configure backend API endpoint, 
 * it's already set in js/create.js as API_BASE
 */

// No frontend configuration needed for AI features
// The backend handles all AI API calls securely
